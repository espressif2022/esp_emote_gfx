# 动态图形资产组织及运行时渲染方法技术交底

## 1. 发明名称

一种动态图形资产组织及运行时渲染方法、装置和存储介质。

## 2. 技术领域

本方案涉及嵌入式图形显示技术，具体涉及一种将动态图形组织为控制点、图形段、姿态和动作步骤，并在运行时转换为多个网格图像对象进行渲染的方法。

## 3. 背景技术

嵌入式显示设备通常具有存储空间有限、处理能力有限以及显示尺寸多样等特点。动态图形可能同时包含线状部件、环状部件、曲线轮廓、填充区域和纹理区域，并需要按照动作或表情连续变化。

动态图形资产需要解决以下问题：如何以统一的数据结构描述不同形态的图形部件；如何使多个图形部件共享姿态数据；以及如何在运行时将动作数据转换为适合嵌入式显示设备执行的渲染数据。

## 4. 要解决的技术问题

提供一种动态图形资产组织及运行时渲染方案，使多种图形部件能够由统一的姿态控制点驱动，并根据各自类型生成对应的网格数据，从而降低动作数据的重复量，并支持动态图形在不同画布尺寸下进行连续渲染。

## 5. 技术方案

### 5.1 动态图形资产组织方式

动态图形资产包括：

- 控制点集合，用于建立动态图形的坐标索引；
- 图形段集合，每个图形段包含图形段类型和控制点引用信息；
- 姿态集合，每个姿态记录控制点在对应状态下的目标坐标；
- 动作步骤集合，每个动作步骤用于指定目标姿态和播放参数；
- 布局参数，用于记录视图区域、默认线宽和动作更新参数；
- 可选的颜色表、图像资源表和叠加图形集合。

控制点集合建立统一的控制点索引。各姿态按照所述统一控制点索引记录目标坐标；动作步骤引用姿态；图形段引用一个或多个控制点索引，并记录图形段类型。由此形成“动作步骤—姿态—控制点—图形段”的引用链。

同一控制点能够被一个或多个图形段引用。图形段通过图形段类型、控制点引用和可选渲染资源描述可动态变化的图形部件。

图形段类型可以包括胶囊段、圆环段、贝塞尔描边段、贝塞尔闭合段和贝塞尔填充段。不同图形段类型对应不同的控制点解释规则和网格生成规则。

资产数据关系可表示为：

```text
动作步骤 -> 目标姿态 -> 统一索引的控制点坐标
                              |
图形段类型 + 控制点索引 ------+
             |
             v
     网格拓扑及转换规则
```

### 5.2 运行时渲染流程

运行时执行以下步骤：

1. 读取动态图形资产，并按照图形段集合预先创建与各图形段对应的网格图像对象，建立图形段与网格图像对象的对应关系；
2. 根据当前动作步骤，从姿态集合中确定目标姿态；
3. 按照统一控制点索引，根据当前姿态坐标和目标姿态坐标更新本次渲染使用的当前控制点坐标；
4. 针对每个图形段，根据其控制点引用信息取得相应的当前控制点坐标；
5. 根据该图形段的图形段类型选择对应的网格拓扑及转换规则，并将所引用的当前控制点坐标转换为网格控制点；
6. 将生成的网格控制点写入该图形段对应的网格图像对象，并绑定颜色或图像资源；
7. 组合多个网格图像对象，得到当前动态图形帧；
8. 在后续动态图形帧中保留并复用所述网格图像对象，在动作步骤、当前控制点、画布或显示状态发生变化时更新网格数据并触发显示刷新。

### 5.3 可选实施特征

- 根据圆环半径确定圆环段的网格列数；
- 按照 `3k+1` 控制点组织贝塞尔曲线段；
- 对闭合曲线生成填充网格或执行扫描线填充；
- 通过资源索引和参考姿态控制点建立纹理坐标；
- 在同一图形段体系中绑定运行时颜色、颜色表颜色或图像纹理；
- 由动作步骤指定叠加图形的索引、位置和缩放比例；
- 将资产视图区域按比例映射到目标画布，并同步缩放坐标、线宽和半径；
- 根据图形段可见层标识选择参与当前帧渲染的图形段；
- 仅在姿态、画布、资源或可见状态变化时更新网格数据。

## 6. 有益效果

本方案通过统一控制点索引以及控制点集合、图形段集合、姿态集合和动作步骤集合之间的引用关系组织动态图形资产，使多个不同类型的图形段能够共享姿态控制点。运行时根据图形段类型选择网格拓扑和转换规则，并通过对应的网格图像对象完成组合渲染，可减少重复坐标数据和逐帧图形数据的存储量。

网格图像对象在初始化阶段按照图形段创建，并在连续动态图形帧之间保留和复用，从而减少高频播放过程中重复创建和释放渲染对象的处理开销。该组织方式还能够在不改变动作和姿态数据的情况下，为图形段选择纯色、颜色表或纹理资源，并通过视图区域到画布的坐标映射适配不同显示尺寸。

## 7. 具体实施例

一个动态图形角色包含头部、肢体、面部轮廓和状态图标。头部配置为圆环段，肢体配置为胶囊段，眉眼和嘴部配置为贝塞尔描边段或填充段。各图形段引用控制点集合中的对应控制点。

姿态集合按照相同的控制点索引顺序，记录静止、挥手和不同表情状态下的控制点坐标。动作步骤依次引用目标姿态，并记录保持时间以及可选状态图标。运行时根据统一索引更新当前控制点，根据各图形段的类型选择圆环、胶囊或贝塞尔网格生成规则，再将生成的网格控制点写入各自跨帧复用的网格图像对象进行组合显示。

当显示画布尺寸发生变化时，根据资产视图区域重新计算映射比例和居中偏移，并据此更新控制点坐标、线宽及半径。动作步骤和姿态数据无需重新生成。

## 8. 建议保护的核心技术特征

独立权利要求建议围绕以下组合关系展开：

1. 动态图形资产包括控制点集合、图形段集合、姿态集合和动作步骤集合；
2. 姿态按照统一控制点索引记录控制点坐标，动作步骤引用目标姿态；
3. 图形段包含图形段类型和控制点索引引用信息；
4. 根据动作步骤引用的目标姿态更新统一索引下的当前控制点；
5. 根据图形段类型选择对应的网格拓扑及转换规则，并根据其引用的当前控制点生成网格控制点；
6. 每个图形段具有预先创建并在连续动态图形帧之间复用的网格图像对象；
7. 将多个网格图像对象组合渲染为动态图形帧。

圆环网格密度、贝塞尔控制点格式、填充方式、纹理参考点、图标叠加、画布映射、分层显示和按变化更新等内容，适合作为从属权利要求。

## 9. 独立权利要求草案

一种动态图形渲染方法，其特征在于，包括：

获取动态图形资产，所述动态图形资产包括控制点集合、图形段集合、姿态集合和动作步骤集合，其中，所述控制点集合建立统一控制点索引，各姿态按照所述统一控制点索引记录控制点坐标，每一动作步骤包括目标姿态引用信息，每一图形段包括图形段类型和控制点索引引用信息；

按照所述图形段集合预先创建与各图形段对应的网格图像对象；

根据当前动作步骤的目标姿态引用信息，从所述姿态集合中确定目标姿态，并按照所述统一控制点索引更新当前控制点坐标；

针对每一图形段，根据该图形段的图形段类型选择对应的网格拓扑及转换规则，并根据该图形段所引用的当前控制点坐标，生成与该图形段对应的网格控制点；

将所述网格控制点写入与该图形段对应的网格图像对象；

通过多个所述网格图像对象渲染得到动态图形帧，并在后续动态图形帧中复用所述网格图像对象。

## 10. 交给代理人的说明

本交底的主要保护对象是统一控制点索引下各数据集合之间的组织和引用关系，以及运行时根据图形段类型选择网格拓扑和转换规则、将网格数据写入跨帧复用对象并组合渲染的处理流程。

具体图元算法和参数用于说明可实施性，并可形成从属权利要求。正式申请时可同时布局方法权利要求、动态图形渲染装置权利要求、电子设备权利要求和计算机可读存储介质权利要求。
