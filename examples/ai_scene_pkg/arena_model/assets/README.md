# arena_model/assets

Staging assets for arena experiments.

Current files are not referenced by CMake targets or ESP demos. The active
playground assets come from `examples/assets/format` via `ARENA_PG_ASSETS_DIR`.

Keep files here only when they are intentionally used by a future arena demo;
otherwise prefer moving them next to the owning demo or deleting them.
